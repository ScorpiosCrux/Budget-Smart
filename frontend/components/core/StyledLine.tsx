import { themes } from "@/theme";
import styled from "styled-components";

const StyledLine = styled.div`
	width: 100%;
	border-top: 1px ${themes.light.secondaryBackground} solid;
`;

export default StyledLine;
