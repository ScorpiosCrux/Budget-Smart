version: "0.01"

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
  express:
    build: backend/.
    ports:
      - 5070:5070
  frontend:
    build: frontend/.
    ports:
      - 5071:3000
